const HOST = 'http://localhost:3000'
const LATEST_POSTS = `${HOST}/api/latest`
const ONE_POST = `${HOST}/api/posts`
const ALL_POSTS = `${HOST}/api/allposts`
const POSTS_BY_TAGNAME = `${HOST}/api/postsByTagname`
const NEW_POST = `${HOST}/api/articlesToHtml`
export {
  LATEST_POSTS,
  ONE_POST,
  ALL_POSTS,
  POSTS_BY_TAGNAME,
  NEW_POST
}
